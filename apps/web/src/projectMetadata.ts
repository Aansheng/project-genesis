/**
 * Current application release metadata.
 *
 * This is the single Web product source for engineering/build values shown by
 * Observatory and attached to current generation context. Runtime gameplay
 * state is intentionally separate from this immutable metadata.
 */
export const PROJECT_METADATA = Object.freeze({
  architectureVersion: 'v1.184',
  currentSprint: 'Sprint 34',
})
