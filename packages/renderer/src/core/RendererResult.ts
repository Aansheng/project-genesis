/**
 * RendererResult — typed result returned by renderer operations.
 *
 * Properties:
 *   - `success`: whether the operation completed without errors
 */

export interface RendererResult {
  readonly success: boolean
}